<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Modern Typography System -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Typography System - Modern eCommerce */
        :root {
            --font-heading: 'Poppins', -apple-system, BlinkMacSystemFont, sans-serif;
            --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            --text-hero: clamp(2.5rem, 5vw + 1rem, 4rem);
            --text-h1: clamp(2rem, 4vw + 0.5rem, 3rem);
            --text-h2: clamp(1.75rem, 3vw + 0.5rem, 2.5rem);
            --text-h3: clamp(1.5rem, 2.5vw + 0.5rem, 2rem);
            --text-h4: clamp(1.25rem, 2vw + 0.25rem, 1.5rem);
            --text-product: clamp(1rem, 1.5vw + 0.25rem, 1.25rem);
            --text-body: clamp(1rem, 1vw + 0.25rem, 1.125rem);
            --text-small: clamp(0.875rem, 0.75vw + 0.25rem, 1rem);
            --lh-tight: 1.1;
            --lh-snug: 1.2;
            --lh-normal: 1.5;
            --lh-relaxed: 1.6;
        }
        * {
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            text-rendering: optimizeLegibility;
        }
        html { font-size: 16px; scroll-behavior: smooth; }
        body {
            font-family: var(--font-body);
            font-size: var(--text-body);
            font-weight: 400;
            line-height: var(--lh-relaxed);
            color: #1f2937;
        }
        h1, h2, h3, h4, h5, h6 {
            font-family: var(--font-heading);
            font-weight: 600;
            line-height: var(--lh-snug);
            letter-spacing: -0.02em;
            color: #111827;
        }
        h1 { font-size: var(--text-h1); font-weight: 700; line-height: var(--lh-tight); }
        h2 { font-size: var(--text-h2); }
        h3 { font-size: var(--text-h3); }
        h4 { font-size: var(--text-h4); font-weight: 500; }
        .hero-heading {
            font-family: var(--font-heading);
            font-size: var(--text-hero);
            font-weight: 700;
            line-height: var(--lh-tight);
            letter-spacing: -0.02em;
        }
        .product-title {
            font-family: var(--font-heading);
            font-size: var(--text-product);
            font-weight: 500;
            line-height: var(--lh-normal);
        }
        p { margin-bottom: 1em; }
        button, .btn {
            font-family: var(--font-body);
            font-weight: 600;
            letter-spacing: 0.025em;
        }
        @media (max-width: 767px) {
            html { font-size: 14px; }
            body { line-height: 1.8; }
        }
    </style>
</head>
<body>
    {{{body}}}
</body>
</html>
