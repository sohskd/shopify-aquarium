<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    
    <!-- Preload critical images -->
    <link rel="preload" as="image" href="/images/image1.jpg">
    <link rel="preload" as="image" href="/images/image2.jpg">
    <link rel="preload" as="image" href="/images/image3.jpg">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --ocean-deep: #0A2540;
            --ocean-blue: #0EA5E9;
            --turquoise: #14B8A6;
            --coral: #FF6B6B;
            --light-blue: #F0F9FF;
        }
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(to bottom, #F0F9FF 0%, #ffffff 100%);
        }
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Poppins', sans-serif;
        }
        
        /* Floating Bubbles Animation - Optimized */
        @keyframes float {
            0%, 100% { transform: translateY(0) translateX(0); opacity: 0.3; }
            50% { transform: translateY(-15px) translateX(8px); opacity: 0.6; }
        }
        .bubble {
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.6), rgba(14,165,233,0.2));
            animation: float 8s ease-in-out infinite;
            will-change: transform, opacity;
        }
        .bubble:nth-child(1) { width: 60px; height: 60px; left: 10%; top: 20%; animation-delay: 0s; }
        .bubble:nth-child(2) { width: 50px; height: 50px; left: 70%; top: 40%; animation-delay: 2s; }
        .bubble:nth-child(3) { width: 70px; height: 70px; left: 40%; top: 60%; animation-delay: 4s; display: none; }
        .bubble:nth-child(4) { width: 40px; height: 40px; left: 85%; top: 70%; animation-delay: 1s; display: none; }
        
        @media (min-width: 768px) {
            .bubble:nth-child(3), .bubble:nth-child(4) { display: block; }
        }
        
        /* Carousel Loading State */
        .carousel-slide {
            background-size: cover;
            background-position: center;
            will-change: opacity;
        }
        .carousel-loading {
            background: linear-gradient(135deg, #0c4a6e 0%, #0891b2 100%);
        }
        
        /* Wave Animation */
        @keyframes wave {
            0%, 100% { transform: translateX(0); }
            50% { transform: translateX(-25px); }
        }
        .wave-svg { animation: wave 10s ease-in-out infinite; }
        
        /* Smooth Scroll */
        html { scroll-behavior: smooth; }
        
        /* Glass Morphism Nav */
        .glass-nav {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(14, 165, 233, 0.1);
        }
        .glass-nav.scrolled {
            background: rgba(10, 37, 64, 0.95);
            color: white;
        }
        
        /* Product Card Hover */
        .product-card {
            transition: all 0.3s ease;
            will-change: transform;
        }
        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(14, 165, 233, 0.3);
        }
        
        /* Performance Optimizations */
        img {
            image-rendering: -webkit-optimize-contrast;
            image-rendering: crisp-edges;
        }
        
        /* Reduce motion for accessibility */
        @media (prefers-reduced-motion: reduce) {
            *, *::before, *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }
    </style>
</head>
<body class="bg-gradient-to-b from-blue-50 to-white">
    <!-- Navigation Header -->
    <nav id="mainNav" class="glass-nav fixed w-full top-0 z-50 transition-all duration-300">
        <div class="container mx-auto px-4">
            <!-- Top bar with logo and icons -->
            <div class="flex items-center justify-between py-5">
                <div class="flex-1"></div>
                <div class="text-center">
                    <h1 class="text-3xl font-semibold tracking-wider bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">AQUATIC AVENUE</h1>
                    <p class="text-xs text-cyan-600 tracking-widest mt-1">DIVE INTO WONDER</p>
                </div>
                <div class="flex-1 flex justify-end items-center space-x-3">
                    <button class="p-2.5 hover:bg-cyan-50 rounded-full transition-colors">
                        <svg class="w-5 h-5 text-cyan-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </button>
                    <button class="p-2.5 hover:bg-cyan-50 rounded-full transition-colors">
                        <svg class="w-5 h-5 text-cyan-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                    </button>
                    <a href="/cart" class="relative p-2.5 hover:bg-cyan-50 rounded-full transition-colors">
                        <svg class="w-5 h-5 text-cyan-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                        </svg>
                        <span id="cartCount" class="absolute -top-1 -right-1 bg-gradient-to-r from-coral-500 to-orange-500 text-white text-[10px] rounded-full px-1.5 py-0.5 shadow-lg">0</span>
                    </a>
                </div>
            </div>
            
            <!-- Navigation Menu -->
            <div class="flex items-center justify-center space-x-10 py-4 text-sm border-t border-cyan-100">
                <a href="/" class="text-cyan-700 hover:text-cyan-900 font-medium relative group">
                    Home
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-cyan-500 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                <a href="#exhibits" class="text-cyan-600 hover:text-cyan-900 relative group">
                    Exhibits
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-cyan-500 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                <a href="/products" class="text-cyan-600 hover:text-cyan-900 relative group">
                    Shop All
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-cyan-500 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                <a href="#about" class="text-cyan-600 hover:text-cyan-900 relative group">
                    About Us
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-cyan-500 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                <a href="#contact" class="text-cyan-600 hover:text-cyan-900 relative group">
                    Visit
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-gradient-to-r from-cyan-500 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
            </div>
        </div>
    </nav>

    <!-- Spacer for fixed nav -->
    <div class="h-32"></div>

    <!-- Hero Carousel Section -->
    <div id="heroCarousel" class="relative h-[600px] overflow-hidden bg-gradient-to-b from-cyan-900 via-blue-800 to-cyan-700">
        <!-- Floating Bubbles -->
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        
        <!-- Slide 1 -->
        <div class="carousel-slide absolute inset-0 transition-opacity duration-700 opacity-100 bg-cover bg-center" style="background-image: linear-gradient(rgba(10,37,64,0.7), rgba(6,182,212,0.6)), url('/images/image1.jpg');">
            <div class="container mx-auto px-4 md:px-8 lg:px-16 h-full flex items-center">
                <div class="text-white max-w-2xl">
                    <h2 class="text-5xl md:text-6xl font-bold mb-6 leading-tight">Dive Into Wonder at <span class="text-cyan-300">Aquatic Avenue</span></h2>
                    <p class="text-xl md:text-2xl mb-10 leading-relaxed text-cyan-50">Explore the depths of aquatic beauty. Premium plants, expert care, and everything you need for a thriving underwater world.</p>
                    <div class="flex flex-wrap gap-4">
                        <a href="#products" class="bg-gradient-to-r from-cyan-500 to-blue-500 text-white px-8 py-4 rounded-full font-semibold hover:shadow-2xl hover:scale-105 transition-all duration-300">
                            Explore Products
                        </a>
                        <a href="#contact" class="border-2 border-cyan-300 text-cyan-100 px-8 py-4 rounded-full font-semibold hover:bg-cyan-300 hover:text-cyan-900 transition-all duration-300">
                            Plan Your Visit
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 2 -->
        <div class="carousel-slide absolute inset-0 transition-opacity duration-700 opacity-0 bg-cover bg-center" style="background-image: linear-gradient(rgba(10,37,64,0.7), rgba(20,184,166,0.6)), url('/images/image2.jpg');">
            <div class="container mx-auto px-4 md:px-8 lg:px-16 h-full flex items-center">
                <div class="text-white max-w-2xl">
                    <h2 class="text-5xl md:text-6xl font-bold mb-6 leading-tight">Premium <span class="text-teal-300">Aquarium Solutions</span></h2>
                    <p class="text-xl md:text-2xl mb-10 leading-relaxed text-teal-50">From custom tanks to expert maintenance, we provide comprehensive aquarium services tailored to your aquatic dreams.</p>
                    <div class="flex flex-wrap gap-4">
                        <a href="#about" class="bg-gradient-to-r from-teal-500 to-cyan-500 text-white px-8 py-4 rounded-full font-semibold hover:shadow-2xl hover:scale-105 transition-all duration-300">
                            Learn More
                        </a>
                        <a href="#products" class="border-2 border-teal-300 text-teal-100 px-8 py-4 rounded-full font-semibold hover:bg-teal-300 hover:text-teal-900 transition-all duration-300">
                            Shop Now
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 3 -->
        <div class="carousel-slide absolute inset-0 transition-opacity duration-700 opacity-0 bg-cover bg-center" style="background-image: linear-gradient(rgba(10,37,64,0.7), rgba(14,165,233,0.6)), url('/images/image3.jpg');">
            <div class="container mx-auto px-4 md:px-8 lg:px-16 h-full flex items-center">
                <div class="text-white max-w-2xl">
                    <h2 class="text-5xl md:text-6xl font-bold mb-6 leading-tight">Discover <span class="text-blue-300">Quality Aquatic Life</span></h2>
                    <p class="text-xl md:text-2xl mb-10 leading-relaxed text-blue-50">Healthy fish, vibrant plants, and premium equipment. Everything you need to create your perfect underwater paradise.</p>
                    <div class="flex flex-wrap gap-4">
                        <a href="#products" class="bg-gradient-to-r from-blue-500 to-cyan-500 text-white px-8 py-4 rounded-full font-semibold hover:shadow-2xl hover:scale-105 transition-all duration-300">
                            Shop Collection
                        </a>
                        <a href="#exhibits" class="border-2 border-blue-300 text-blue-100 px-8 py-4 rounded-full font-semibold hover:bg-blue-300 hover:text-blue-900 transition-all duration-300">
                            View Exhibits
                        </a>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Carousel Controls -->
        <div class="absolute bottom-8 left-0 right-0 flex items-center justify-center space-x-4">
            <button id="prevBtn" class="text-white hover:text-gray-300 transition">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                </svg>
            </button>
            <div id="carouselDots" class="flex space-x-2">
                <button class="carousel-dot w-2 h-2 bg-white rounded-full" data-slide="0"></button>
                <button class="carousel-dot w-2 h-2 bg-gray-400 rounded-full" data-slide="1"></button>
                <button class="carousel-dot w-2 h-2 bg-gray-400 rounded-full" data-slide="2"></button>
            </div>
            <button id="nextBtn" class="text-white hover:text-gray-300 transition">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
            </button>
            <button id="playPauseBtn" class="text-white hover:text-gray-300 ml-4 transition">
                <svg id="pauseIcon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <svg id="playIcon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
            </button>
        </div>
    </div>

    <script>
        // Preload carousel images for smooth transitions
        const carouselImages = ['/images/image1.jpg', '/images/image2.jpg', '/images/image3.jpg'];
        carouselImages.forEach(src => {
            const img = new Image();
            img.src = src;
        });

        // Carousel functionality
        let currentSlide = 0;
        let isPlaying = true;
        let autoPlayInterval;
        const slides = document.querySelectorAll('.carousel-slide');
        const dots = document.querySelectorAll('.carousel-dot');
        const totalSlides = slides.length;

        function showSlide(index) {
            // Wrap around
            if (index >= totalSlides) currentSlide = 0;
            else if (index < 0) currentSlide = totalSlides - 1;
            else currentSlide = index;

            // Update slides with GPU acceleration
            slides.forEach((slide, i) => {
                if (i === currentSlide) {
                    slide.style.opacity = '1';
                    slide.style.zIndex = '10';
                } else {
                    slide.style.opacity = '0';
                    slide.style.zIndex = '1';
                }
            });

            // Update dots
            dots.forEach((dot, i) => {
                if (i === currentSlide) {
                    dot.classList.remove('bg-gray-400');
                    dot.classList.add('bg-white');
                } else {
                    dot.classList.remove('bg-white');
                    dot.classList.add('bg-gray-400');
                }
            });
        }

        function nextSlide() {
            showSlide(currentSlide + 1);
        }

        function prevSlide() {
            showSlide(currentSlide - 1);
        }

        function startAutoPlay() {
            autoPlayInterval = setInterval(nextSlide, 5000); // Change slide every 5 seconds
        }

        function stopAutoPlay() {
            clearInterval(autoPlayInterval);
        }

        function togglePlayPause() {
            isPlaying = !isPlaying;
            const pauseIcon = document.getElementById('pauseIcon');
            const playIcon = document.getElementById('playIcon');

            if (isPlaying) {
                pauseIcon.classList.remove('hidden');
                playIcon.classList.add('hidden');
                startAutoPlay();
            } else {
                pauseIcon.classList.add('hidden');
                playIcon.classList.remove('hidden');
                stopAutoPlay();
            }
        }

        // Event listeners
        document.getElementById('prevBtn').addEventListener('click', () => {
            prevSlide();
            if (isPlaying) {
                stopAutoPlay();
                startAutoPlay(); // Reset timer
            }
        });

        document.getElementById('nextBtn').addEventListener('click', () => {
            nextSlide();
            if (isPlaying) {
                stopAutoPlay();
                startAutoPlay(); // Reset timer
            }
        });

        document.getElementById('playPauseBtn').addEventListener('click', togglePlayPause);

        // Dot navigation
        dots.forEach((dot, index) => {
            dot.addEventListener('click', () => {
                showSlide(index);
                if (isPlaying) {
                    stopAutoPlay();
                    startAutoPlay(); // Reset timer
                }
            });
        });

        // Start autoplay on load
        startAutoPlay();
    </script>

    <script>
        // Navbar cart count on homepage
        (function(){
            const CART_KEY = 'aa_cart';
            function loadCart(){
                try { return JSON.parse(localStorage.getItem(CART_KEY) || '[]'); } catch { return []; }
            }
            function updateCartCount(){
                const items = loadCart();
                const count = items.reduce((a,i)=>a+i.quantity,0);
                const el = document.getElementById('cartCount');
                if (el) el.textContent = String(count);
            }
            updateCartCount();
        })();
    </script>

    <!-- Wave Divider -->
    <div class="relative">
        <svg class="wave-svg w-full h-24" viewBox="0 0 1200 120" preserveAspectRatio="none">
            <path d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0.67,0,0,321.39,56.44Z" fill="#0EA5E9" opacity="0.3"></path>
            <path d="M0,0V5.63C149.93,59,314.09,71.32,475.83,42.57c43-7.64,84.23-20.12,127.61-26.46,59-8.63,112.48,12.24,165.56,35.4C827.93,77.22,886,95.24,951.2,90c86.53-7,172.46-45.71,248.8-84.81V0Z" fill="#0EA5E9" opacity="0.5"></path>
        </svg>
    </div>

    <!-- Mission Statement -->
    <section id="about" class="py-20 bg-gradient-to-b from-cyan-50 to-white relative overflow-hidden">
        <div class="container mx-auto px-4 text-center relative z-10">
            <h2 class="text-4xl md:text-5xl font-bold mb-6 bg-gradient-to-r from-cyan-700 to-blue-700 bg-clip-text text-transparent">Explore the Depths of Aquatic Beauty</h2>
            <p class="text-lg md:text-xl max-w-4xl mx-auto text-gray-700 leading-relaxed">At Aquatic Avenue, we're passionate about making the aquarium hobby accessible to everyone. From beginners to experts, we provide premium plants, equipment, and the knowledge you need to create thriving underwater ecosystems.</p>
        </div>
    </section>

    <!-- Shop Best Sellers / Marine Life Showcase -->
    <section id="products" class="py-20 bg-gradient-to-b from-white to-cyan-50">
        <div class="container mx-auto px-4">
            <div class="text-center mb-16">
                <h2 class="text-4xl md:text-5xl font-bold mb-4 bg-gradient-to-r from-cyan-700 to-blue-700 bg-clip-text text-transparent">Featured Aquatic Collection</h2>
                <p class="text-lg text-gray-600 max-w-2xl mx-auto">Discover our premium selection of aquatic plants and accessories</p>
            </div>
            
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
                {{#each bestSellers}}
                <a href="/product/{{this.id}}" class="product-card group bg-white rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl block">
                    <div class="aspect-square bg-gradient-to-br from-cyan-100 to-blue-100 flex items-center justify-center overflow-hidden relative">
                        {{#if this.image}}
                            {{#if (startsWith this.image "http")}}
                                <img src="{{this.image}}&width=400" alt="{{this.name}}" loading="lazy" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                            {{else}}
                                <img src="/images/{{this.image}}" alt="{{this.name}}" loading="lazy" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500">
                            {{/if}}
                        {{else}}
                            <span class="text-cyan-400">🌿 No Image</span>
                        {{/if}}
                        <div class="absolute inset-0 bg-gradient-to-t from-cyan-900/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-base font-semibold text-gray-800 mb-2 group-hover:text-cyan-700 transition-colors">{{this.name}}</h3>
                        <p class="text-xl font-bold bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">{{this.price}}</p>
                    </div>
                </a>
                {{/each}}
            </div>

            <!-- View All Button -->
            <div class="text-center">
                <a href="/products" class="inline-block bg-gradient-to-r from-cyan-500 to-blue-500 text-white px-10 py-4 rounded-full font-semibold hover:shadow-2xl hover:scale-105 transition-all duration-300">
                    View All Products
                </a>
            </div>
        </div>
    </section>

    <!-- Features / Highlights Section -->
    <section id="exhibits" class="py-20 bg-white">
        <div class="container mx-auto px-4">
            <div class="grid md:grid-cols-3 gap-10">
                <div class="bg-gradient-to-br from-cyan-50 to-blue-50 p-8 rounded-2xl text-center hover:shadow-xl transition-shadow duration-300">
                    <div class="text-6xl mb-6">🐠</div>
                    <h3 class="text-2xl font-bold mb-4 text-cyan-800">Premium Aquatic Life</h3>
                    <p class="text-gray-700 leading-relaxed">Healthy, vibrant plants and fish from trusted suppliers. Each specimen carefully selected for quality and vitality.</p>
                </div>
                <div class="bg-gradient-to-br from-teal-50 to-cyan-50 p-8 rounded-2xl text-center hover:shadow-xl transition-shadow duration-300">
                    <div class="text-6xl mb-6">🔧</div>
                    <h3 class="text-2xl font-bold mb-4 text-teal-800">Expert Guidance</h3>
                    <p class="text-gray-700 leading-relaxed">Professional consultation and maintenance services. Our team helps you create and maintain the perfect aquatic environment.</p>
                </div>
                <div class="bg-gradient-to-br from-blue-50 to-cyan-50 p-8 rounded-2xl text-center hover:shadow-xl transition-shadow duration-300">
                    <div class="text-6xl mb-6">🏆</div>
                    <h3 class="text-2xl font-bold mb-4 text-blue-800">Quality Guarantee</h3>
                    <p class="text-gray-700 leading-relaxed">Industry-leading warranty on all equipment. Your satisfaction and success are our top priorities.</p>
                </div>
            </div>
        </div>
    </section>


    <!-- CTA Section -->
    <section id="contact" class="py-24 bg-gradient-to-br from-cyan-600 via-blue-600 to-teal-600 text-white relative overflow-hidden">
        <!-- Decorative Bubbles -->
        <div class="absolute top-10 left-10 w-32 h-32 bg-white/10 rounded-full blur-2xl"></div>
        <div class="absolute bottom-10 right-10 w-40 h-40 bg-white/10 rounded-full blur-2xl"></div>
        
        <div class="container mx-auto px-4 text-center relative z-10">
            <h2 class="text-4xl md:text-5xl font-bold mb-6">Ready to Dive In?</h2>
            <p class="text-xl md:text-2xl mb-10 max-w-3xl mx-auto leading-relaxed">Our team of aquatic specialists is here to help you create and maintain the perfect underwater ecosystem. Let's bring your aquarium dreams to life!</p>
            <div class="flex flex-wrap justify-center gap-4">
                <a href="#products" class="bg-white text-cyan-700 px-10 py-4 rounded-full font-bold hover:bg-cyan-50 hover:scale-105 transition-all duration-300 shadow-xl">
                    Shop Now
                </a>
                <a href="tel:+6561234567" class="border-2 border-white text-white px-10 py-4 rounded-full font-bold hover:bg-white hover:text-cyan-700 transition-all duration-300">
                    Contact Us
                </a>
            </div>
        </div>
    </section>

    <!-- Wave Divider Before Footer -->
    <div class="relative bg-white">
        <svg class="wave-svg w-full h-24" viewBox="0 0 1200 120" preserveAspectRatio="none">
            <path d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0.67,0,0,321.39,56.44Z" fill="#0A2540" opacity="1"></path>
        </svg>
    </div>

    <!-- Footer -->
    <footer class="bg-gradient-to-b from-slate-900 to-cyan-950 text-white py-16">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-10">
                <div>
                    <h3 class="text-2xl font-bold mb-4 bg-gradient-to-r from-cyan-400 to-blue-400 bg-clip-text text-transparent">Aquatic Avenue</h3>
                    <p class="text-cyan-200 leading-relaxed">Singapore's premier aquarium specialist. Dive into wonder with us.</p>
                </div>
                <div>
                    <h4 class="font-bold mb-4 text-cyan-300">Quick Links</h4>
                    <ul class="space-y-3">
                        <li><a href="#products" class="text-cyan-100 hover:text-cyan-300 transition-colors">Shop</a></li>
                        <li><a href="#exhibits" class="text-cyan-100 hover:text-cyan-300 transition-colors">Exhibits</a></li>
                        <li><a href="#about" class="text-cyan-100 hover:text-cyan-300 transition-colors">About Us</a></li>
                        <li><a href="#contact" class="text-cyan-100 hover:text-cyan-300 transition-colors">Contact</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-bold mb-4 text-cyan-300">Visit Us</h4>
                    <ul class="space-y-3 text-cyan-100">
                        <li>📍 123 Aqua Street</li>
                        <li>Singapore 123456</li>
                        <li>📞 +65 6123 4567</li>
                        <li>✉️ hello@aquaticavenue.com</li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-bold mb-4 text-cyan-300">Follow Our Journey</h4>
                    <div class="flex space-x-4">
                        <a href="#" class="w-10 h-10 bg-cyan-700 hover:bg-cyan-600 rounded-full flex items-center justify-center transition-colors">FB</a>
                        <a href="#" class="w-10 h-10 bg-cyan-700 hover:bg-cyan-600 rounded-full flex items-center justify-center transition-colors">IG</a>
                        <a href="#" class="w-10 h-10 bg-cyan-700 hover:bg-cyan-600 rounded-full flex items-center justify-center transition-colors">YT</a>
                    </div>
                </div>
            </div>
            <div class="border-t border-cyan-800 mt-12 pt-8 text-center text-cyan-300">
                <p>&copy; 2025 Aquatic Avenue. All rights reserved. | Crafted with 💙 for aquarium enthusiasts</p>
            </div>
        </div>
    </footer>
    
    <!-- Scroll to Top Navigation Script -->
    <script>
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('mainNav');
            if (window.scrollY > 100) {
                nav.classList.add('scrolled');
            } else {
                nav.classList.remove('scrolled');
            }
        });
    </script>
</body>
</html>
