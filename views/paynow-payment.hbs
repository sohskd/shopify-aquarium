<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(to bottom, #F0F9FF 0%, #ffffff 100%);
        }
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Poppins', sans-serif;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        .pulse { animation: pulse 2s ease-in-out infinite; }
    </style>
</head>
<body class="bg-gradient-to-b from-blue-50 to-white min-h-screen">
    <div class="container mx-auto px-4 py-16 max-w-3xl">
        <!-- Header -->
        <div class="text-center mb-8">
            <a href="/" class="inline-block mb-4">
                <h1 class="text-3xl font-semibold tracking-wider bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent">AQUATIC AVENUE</h1>
            </a>
            <h2 class="text-2xl md:text-3xl font-bold text-gray-800 mb-2">Scan to Pay with PayNow</h2>
            <p class="text-gray-600">Use your banking app to scan the QR code below</p>
        </div>

        <!-- Payment Card -->
        <div class="bg-white rounded-3xl shadow-2xl p-8 md:p-12">
            {{#if error}}
                <!-- Error State -->
                <div class="text-center">
                    <div class="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
                        <svg class="w-10 h-10 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </div>
                    <h3 class="text-xl font-bold text-gray-800 mb-4">Unable to Generate QR Code</h3>
                    <p class="text-gray-600 mb-6">{{error}}</p>
                    <a href="/checkout/payment" class="inline-block bg-gradient-to-r from-cyan-500 to-blue-500 text-white px-8 py-3 rounded-full font-semibold hover:shadow-xl transition-all duration-300">
                        Try Again
                    </a>
                </div>
            {{else}}
                <!-- QR Code -->
                <div class="text-center mb-8">
                    <div class="bg-gradient-to-br from-teal-50 to-green-50 p-8 rounded-2xl inline-block mb-6">
                        <img src="{{qrCode}}" alt="PayNow QR Code" class="w-64 h-64 mx-auto">
                    </div>
                    <div class="bg-gradient-to-r from-teal-50 to-green-50 p-6 rounded-xl mb-6">
                        <p class="text-sm text-gray-600 mb-2">Amount to Pay</p>
                        <p class="text-4xl font-bold text-teal-700 mb-3">${{amount}} SGD</p>
                        <p class="text-xs text-gray-500">Reference: <span class="font-mono font-semibold">{{reference}}</span></p>
                    </div>
                </div>

                <!-- Instructions -->
                <div class="space-y-4 mb-8">
                    <h3 class="text-lg font-bold text-gray-800 mb-4">How to Pay:</h3>
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 bg-gradient-to-r from-teal-500 to-green-500 rounded-full flex items-center justify-center flex-shrink-0">
                            <span class="text-white font-bold">1</span>
                        </div>
                        <div>
                            <p class="font-semibold text-gray-800">Open Your Banking App</p>
                            <p class="text-sm text-gray-600">DBS, OCBC, UOB, or any Singapore bank app</p>
                        </div>
                    </div>
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 bg-gradient-to-r from-teal-500 to-green-500 rounded-full flex items-center justify-center flex-shrink-0">
                            <span class="text-white font-bold">2</span>
                        </div>
                        <div>
                            <p class="font-semibold text-gray-800">Scan the QR Code</p>
                            <p class="text-sm text-gray-600">Use the PayNow or QR scanner feature</p>
                        </div>
                    </div>
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 bg-gradient-to-r from-teal-500 to-green-500 rounded-full flex items-center justify-center flex-shrink-0">
                            <span class="text-white font-bold">3</span>
                        </div>
                        <div>
                            <p class="font-semibold text-gray-800">Confirm Payment</p>
                            <p class="text-sm text-gray-600">Verify the amount and complete the transfer</p>
                        </div>
                    </div>
                    <div class="flex items-start space-x-4">
                        <div class="w-10 h-10 bg-gradient-to-r from-teal-500 to-green-500 rounded-full flex items-center justify-center flex-shrink-0">
                            <span class="text-white font-bold">4</span>
                        </div>
                        <div>
                            <p class="font-semibold text-gray-800">Click "I've Paid" Below</p>
                            <p class="text-sm text-gray-600">We'll confirm your payment within 24 hours</p>
                        </div>
                    </div>
                </div>

                <!-- Important Notice -->
                <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-8">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <svg class="h-5 w-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                            </svg>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm text-yellow-700">
                                <strong>Important:</strong> Please include the reference number <span class="font-mono font-semibold">{{reference}}</span> in your payment. This helps us identify your order.
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="space-y-4">
                    <button onclick="confirmPayment()" class="w-full bg-gradient-to-r from-teal-500 to-green-500 text-white px-8 py-4 rounded-full font-bold text-lg hover:shadow-2xl hover:scale-105 transition-all duration-300">
                        I've Completed Payment
                    </button>
                    <a href="/checkout/payment" class="block w-full text-center text-gray-600 hover:text-gray-800 font-medium py-3">
                        ← Choose Different Payment Method
                    </a>
                </div>
            {{/if}}
        </div>

        <!-- Help Section -->
        <div class="text-center mt-8 text-gray-600">
            <p class="text-sm">Need help? Email us at <a href="mailto:hello@aquaticavenue.com" class="text-cyan-600 hover:text-cyan-800 font-medium">hello@aquaticavenue.com</a></p>
        </div>
    </div>

    <script>
        const CART_KEY = 'aa_cart';
        const reference = '{{reference}}';

        function confirmPayment() {
            // Clear cart
            localStorage.removeItem(CART_KEY);
            
            // Show confirmation message
            if (confirm('Have you completed the PayNow transfer?\n\nWe will verify your payment within 24 hours and send you a confirmation email.')) {
                // Redirect to success page
                window.location.href = `/payment/success?ref=${reference}`;
            }
        }
    </script>
</body>
</html>
